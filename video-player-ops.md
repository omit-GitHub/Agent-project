# 视频播放器原子操作清单（用现有 op 组合）

> 本文档整理「点开片源进入播放器后」的视频相关原子操作。这些操作**无需改协议**，全部由 GUIAgent v1.2 已有的 16 个原子 op 组合实现——核心是 `find`（查节点）+ `click_node`（点按钮）+ `tap`（点区域）+ `scroll_node`（翻页）+ `global`（系统动作）。
>
> 与 `run-search.py` 同构：换个被控 APP 只需替换序列里的 res-id（用 `dump` 查当前页面拿到），GUIAgent 本身没有也不需要单独的「视频控制」宏 op。

## 0. 前置

- 设备已开 GUIAgent 无障碍服务（ws 服务随 `onServiceConnected` 常驻，监听 `0.0.0.0:8322`），PC 直连设备填 `GUIAGENT_WS_HOST=<设备IP>`，或经 `adb forward tcp:8322 tcp:8322` 用默认 host。
- 已通过 `run-search.py` + `run-play.py` 进入某片源播放器。
- 下文 `<find>` 表示 `find` 的子集条件 `{text?,id?,desc?,cls?,nid?}`；`<id>` 表示该 APP 里某按钮的 resource-id，需用 `dump` 实测获取。

## 1. 操作清单

### 1.1 播放 / 暂停

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `tap` | `{x: 屏宽/2, y: 屏高/2}` | 点播放器中心唤出控制条/切换播放 |
| 2 | `click_node` | `{id:<播放按钮id>, index:0}` | 若中心点击不触发，改点显式播放按钮 |

> 多数播放器点中心区域即切换播放/暂停；少数要点显式按钮。先 `dump` 看当前页是否有可点的播放/暂停按钮（`desc` 常含「播放」「暂停」）。

### 1.2 上一集 / 下一集

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `tap` | `{x: 屏宽/2, y: 屏高/2}` | 唤出控制条（上一集/下一集按钮多在控制条） |
| 2 | `click_node` | `{id:<上一集id>}` 或 `{id:<下一集id>}` | `desc` 常为「上一集」「下一集」 |

### 1.3 选集 / 选季

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `tap` | 中心 | 唤出控制条 |
| 2 | `click_node` | `{id:<选集入口id>}` | 打开选集面板 |
| 3 | `find` | `{id:<集条目id>, limit:50}` | 读集列表拿 `nid`/`bounds` |
| 4 | `scroll_node` | `{id:<集列表容器id>, dir:"down"}` | 集数多时翻页 |
| 5 | `click_node` | `{nid:<第n集nid>}` 或 `tap` 其 `bounds` 中心 | 选定某集 |

> `click_node` 的 `index` 是**树前序**非视觉序，多列网格选第 n 个时不可靠；参考 `run-play.py` 的行优先排序后用 `tap` 坐标点中心更稳。

### 1.4 倍速（1x / 1.25x / 1.5x / 2x）

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `tap` | 中心 | 唤出控制条 |
| 2 | `click_node` | `{id:<倍速按钮id>}` | 弹出倍速档位 |
| 3 | `click_node` | `{text:"1.5x"}` 或 `{id:<档位id>}` | 选定档位 |

### 1.5 清晰度切换

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `tap` | 中心 | 唤出控制条 |
| 2 | `click_node` | `{id:<清晰度按钮id>}` | 弹出清晰度列表（常标「1080P」「蓝光」） |
| 3 | `click_node` | `{text:"1080P"}` | 选定清晰度 |

### 1.6 字幕 / 音轨

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `tap` | 中心 | 唤出控制条 |
| 2 | `click_node` | `{id:<字幕按钮id>}` 或 `{id:<音轨按钮id>}` | 打开字幕/音轨面板 |
| 3 | `click_node` | `{text:"中文字幕"}` 等 | 选定条目 |

### 1.7 弹幕开关

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `tap` | 中心 | 唤出控制条 |
| 2 | `click_node` | `{id:<弹幕按钮id>}` | `desc` 常含「弹幕」；切换开/关 |

### 1.8 全屏 / 退出全屏

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `tap` | 中心 | 唤出控制条 |
| 2 | `click_node` | `{id:<全屏按钮id>}` | `desc` 常含「全屏」「退出全屏」 |

### 1.9 锁屏（防误触）

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `tap` | 中心 | 唤出控制条 |
| 2 | `click_node` | `{id:<锁屏按钮id>}` | 锁定后只响应解锁按钮 |

### 1.10 跳过片头 / 片尾

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `click_node` | `{id:<跳过按钮id>}` | 片头/片尾出现时按钮才渲染，需轮询 `find` |

> 「跳过」按钮是条件渲染的，时机性强：建议 `find` 重试（参考 `run-play.py` 的 `find_retry`），命中即点。

### 1.11 长按倍速（临时 3x）

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `long_press` | `{x: 屏宽/2, y: 屏高/2, duration:1000}` | 长按播放器中心触发临时倍速 |

### 1.12 退出播放器

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `global` | `{action:"back"}` | 退一层；全屏播放可能需多次 back 才退到列表 |

### 1.13 读当前进度 / 总时长

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `tap` | 中心 | 唤出控制条（进度文本才显示） |
| 2 | `find` | `{id:<当前时间id>}` / `{id:<总时长id>}` | 读返回节点的 `text` |

> 这是**读屏**而非动作，进度文本是 `TextView` 节点。读「是否在播」需找播放/暂停按钮的 `desc`/图标猜，不准——精确读状态需走 `media_state` op（见协议扩展清单，不在本文档范围）。

### 1.14 调节音量（右侧垂直慢滑）

屏幕**右侧**垂直慢滑（`swipe`）模拟播放器手势：上滑调高、下滑调低。多数播放器屏区分工：右侧=音量、左侧=亮度、中间=进度，故必须在右侧；慢滑（`duration` 长）才被识别为音量手势，快滑会触发别的动作。

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `ping` | `{}` | 拿屏幕尺寸算右侧 x |
| 2 | `swipe` | `{x1:w*0.75, y1:h*0.65, x2:w*0.75, y2:h*0.35, duration:700}` | 右侧上滑=调高（一次一格） |
| 2' | `swipe` | `{x1:w*0.75, y1:h*0.35, x2:w*0.75, y2:h*0.65, duration:700}` | 右侧下滑=调低 |

> **仅播放界面可用**；非播放界面此手势无效或触发别的动作。一次手势调一格音量，调多格连发多次 `swipe`。对应脚本 [`run-volume.py`](run-volume.py)。

### 1.15 调节亮度（左侧垂直慢滑）

与音量对称：屏幕**左侧**垂直慢滑，上滑调亮、下滑调暗。

| 步骤 | op | args | 说明 |
|---|---|---|---|
| 1 | `ping` | `{}` | 拿屏幕尺寸算左侧 x |
| 2 | `swipe` | `{x1:w*0.25, y1:h*0.65, x2:w*0.25, y2:h*0.35, duration:700}` | 左侧上滑=调亮 |
| 2' | `swipe` | `{x1:w*0.25, y1:h*0.35, x2:w*0.25, y2:h*0.65, duration:700}` | 左侧下滑=调暗 |

> **仅播放界面可用**。对应脚本 [`run-brightness.py`](run-brightness.py)。音量/亮度都是**手势模拟**，依赖播放器实现了屏区分工手势；不支持该手势的播放器此二项无效。

## 2. 通用模式

视频控制脚本可复用 `run-play.py` 已验证的两个模式：

- **`find_retry`**：播放器控制条、跳过按钮等是条件渲染/带动画的，`find` 瞬时返回空属正常，需带间隔重试。
- **行优先排序后 `tap` 中心**：多列网格（选集墙）用 `click_node` 的 `index` 不可靠，按 `(cy 行, cx 列)` 排序后 `tap` 坐标最稳。

## 3. 换 APP 的改造点

本文档只给操作语义，**res-id 不通用**——换一个播放器 APP，每一步的 `<...id>` 都要重新用 `dump` 抓。改造流程：

1. `start` 拉起目标播放器 APP；
2. `dump {depth:6}` 看当前页 UI 树，定位各按钮的 `id`/`desc`；
3. 把对应 id 填入上文序列；
4. 多列网格类操作（选集）套用 `run-play.py` 的行优先 + `tap`。

> 这正是 GUIAgent 的设计取向：协议层只提供原子 op，APP 适配靠组合序列的 res-id 替换，不把「搜索」「播放控制」做成协议宏 op。

## 4. 边界（这些不在本文档覆盖范围）

以下视频相关操作现有 op 做不到或做不好，属于协议扩展议题，见主梳理文档：

- **任意界面调音量/亮度**：播放界面已可用手势（见 §1.14 / §1.15），但**仅限支持屏区手势的播放器、且仅播放界面**；任意界面、任意播放器调音量仍需 `volume` op（走 `AudioManager`，无 root，未实现），无障碍发不了物理音量键。
- **媒体键**（硬件 play/pause/next/prev）：无障碍无法注入 KeyEvent。
- **精确 seek**：`gesture` 能拖进度条但无「按住」语义保证，可能需新增 `drag` op。
- **`wait(event)`**：协议已占位未实现，「等广告/等加载」需开 `onAccessibilityEvent` 事件流。
- **读播放状态**：`find` 文本猜测不准，需新增 `media_state` op（走 `MediaSessionManager`）。
